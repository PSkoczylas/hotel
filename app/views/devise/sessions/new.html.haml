%div{class: "left-margin"}
  %h2= "Zaloguj się"

  .row
    .col-md-6
      = simple_form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
        %div{class: "form-inputs"}
          = f.input :email, label: "Wpisz adres email:", required: false, autofocus: true
          = f.input :password, label: "Wpisz hasło:", required: false
          = f.input :remember_me, label: "Zapamiętaj mnie", as: :boolean if devise_mapping.rememberable?
          = f.submit "Zaloguj się", class: "btn btn-primary"

  = link_to "Przypomnij hasło", new_password_path(resource_name)
